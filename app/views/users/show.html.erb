<p>
  <strong>Name:</strong>
  <%= @post.name %>
</p>

<p>
  <strong>Avatar:</strong>
  <%= @post.avatar %>
</p>
<p><%= image_tag @post.avatar.thumb.url %></p>
<p><%= @post.body %></p>

<%= link_to 'Edit', edit_user_path(@post) %> |
<%= link_to 'Back', users_path %>
